[HLS 并发下载改造 TODO]

1. 梳理当前流程
- 确认 GenericHLSHandler.download 中串行步骤：下载切片 -> 可选解密 -> 顺序写入 temp ts。
- 确认 UI 与 Manager 透传参数与 headers/cookies 的路径。
- 确认 hls 判断条件与 real_url 来源（main.py 的 format_url_map）。

2. 并发方案设计
- 使用 ThreadPoolExecutor 并发下载切片（建议初始 workers=8，可配置）。
- 任务返回 (index, bytes_data, err)；主线程按 index 顺序写入，保证时序。
- 保留 AES-128 解密：优先在任务线程内解密；如共享 key_cache，需加锁或预拉取 key。
- 先收集 playlist.segments 元信息，避免并发阶段访问 m3u8 对象带来的共享风险。

3. 可靠性与限速策略
- requests.Session 并发访问评估：必要时改为每线程独立 Session 或使用连接池适配器。
- 增加 retry/backoff（429/5xx/超时），限制最大重试次数。
- 提供降级开关：并发失败时可回退串行。
- 允许配置最大并发与超时时间，便于针对站点限流调优。

4. 日志与进度
- 进度日志从“每10片”改为“已完成数/总数（含失败计数）”。
- 保持现有 UI 回调格式，避免界面联动修改过大。
- 记录关键异常：首个失败切片的 URL 与状态码。

5. 验证
- 小流（<100切片）和大流（>1000切片）分别验证。
- 验证加密流与非加密流。
- 验证最终 ffmpeg 合并输出可播放、音画同步。
- 对比并发/串行耗时与失败率，记录样本数据。
